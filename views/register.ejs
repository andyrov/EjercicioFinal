<!DOCTYPE html>
<html>

<head>
    <title>Registro</title>
    <meta name="viewport" content="width=device-wid th">
    <meta charset="utf-8">
</head>

<body>
    <header>

    </header>
    <main>
        <h1>Registro</h1>
        <section>
            <form action="/users/create" method="POST">
                <label for="name">Nombre de usuario:</label>
                <p><input type="text" name="name" id=""></p>
                <% if (locals.errors && errors.name) { %>
                    <p style="color: red"><%= errors.name.msg %></p>
                <% } %>
                <label for="email">Email:</label>
                <p><input type="email" name="email" id=""></p>
                <% if (locals.errors && errors.email) { %>
                    <p style="color: red"><%= errors.email.msg %></p>
                <% } %>
                <label for="password">Contraseña:</label>
                <p><input type="password" name="password" id=""></p>
                <% if (locals.errors && errors.password) { %>
                    <p style="color: red"><%= errors.password.msg %></p>
                <% } %>
                <label for="confirm_password">Confirmar contraseña:</label>
                <p><input type="password" name="confirm_password" id=""></p>
                <% if (locals.errors && errors.confirm_password) { %>
                    <p style="color: red"><%= errors.confirm_password.msg %></p>
                <% } %>
                <p> <button type="submit">Agregar</button> </p>
                <br>
                <a href="/"> Atras</a>
            </form>
        </section>
    </main>
    <footer>

    </footer>
</body>

</html>