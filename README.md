Install dotenv with npm using the following command:

npm install dotenv --save

Add an .env file containing the following lines:

SECRET_KEY="YOURMYSQLPASSWORD"

Put your mysql password between the ""
